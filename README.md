## 📄 Input Mask for CPF and RG

This JavaScript function uses Regular Expressions (`replace`) to automatically format CPF (`###.###.###-##`) and RG (`##.###.###-#`) during user input, improving user experience (UX).

It removes non-numeric characters before saving and enforces maximum length limits, ensuring that the data is stored in the correct format for the database.
